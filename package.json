{
  "name": "miditranslate-workspace",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "tauri:dev": "npm --prefix frontend exec tauri -- dev",
    "tauri:build": "npm --prefix frontend exec tauri -- build"
  }
}
